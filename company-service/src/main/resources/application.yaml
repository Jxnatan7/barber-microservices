# PostgreSQL Properties
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/company
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Mongo Properties
  data:
    mongodb:
      host: localhost
      port: 27017
      username: root
      password: root
      database: haircut-service
